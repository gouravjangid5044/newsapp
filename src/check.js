const floatNum = 3.001;
const nearestIntegerFloor = Math.floor(floatNum);
const nearestIntegerCeil = Math.ceil(floatNum);
console.log(nearestIntegerFloor); // Output: 3
console.log(nearestIntegerCeil); // Output: 4
